## Session: 2026-09-07T23-30

## Canary
Every response MUST start with `Myles - ` (no exceptions). This is in CLAUDE.md. If you notice you've dropped it, call it out and write a new handoff.

## What Happened This Session
Schema column renames + additions to the MLB TB snapshot parquet. Data needs to be deleted from S3 and Lambda redeployed (Myles will do this himself — do NOT deploy).

## Final Snapshot Parquet Schema (6 new/renamed columns)
| Column | Type | Example |
|---|---|---|
| `snapshot_ts_utc` | str (ISO) | `2026-09-07T14:06:00Z` |
| `snapshot_ts_et` | str | `2026-09-07 10:06 AM ET` |
| `season` | int | `2026` |
| `game_date_et` | str | `2026-09-07` ← **primary date, use this for joins** |
| `game_date_utc` | str | `2026-09-08` ← raw UTC date, for reference only |
| `event_id` | str | Odds API event ID |
| `home_team` | str | |
| `away_team` | str | |
| `commence_time_utc` | str (ISO) | `2026-09-08T02:06:00Z` |
| `commence_time_et` | str | `2026-09-07 10:06 PM ET` |
| `bookmaker` | str | |
| `market_key` | str | `batter_total_bases` |
| `player_name` | str | |
| `over_line` | float | |
| `over_american_odds` | int | |
| `under_line` | float | |
| `under_american_odds` | int | |
| `binary_player_game_first_seen` | bool | |
| `last_odds_player_game` | str (JSON) | |
| `credits_before` | int | |
| `credits_after` | int | |

## Files Modified This Session
- `src/mlb_total_bases_modeling/scripts/snapshot_props.py` — all 6 new/renamed columns written
- `src/mlb_total_bases_modeling/scripts/compute_clv.py` — `commence_time` → `commence_time_utc`
- `src/mlb_total_bases_modeling/scripts/smoke_test_snapshots.py` — REQUIRED_COLUMNS updated
- `src/mlb_total_bases_modeling/scripts/verify_snapshot.py` — REQUIRED_COLUMNS updated
- `src/mlb_total_bases_modeling/analysis/plot_clv_snapshot.py` — reads `snapshot_ts_et`/`commence_time_et` from parquet for display; `_utc_str_to_epoch` for epoch math

## Pre-Deploy Checklist (Myles does this)
1. Delete all existing S3 snapshot data:
   ```
   aws s3 rm s3://the-odds-api-mt/mlb/total_bases_model/prop_snapshots/ --recursive
   ```
2. Run deploy script:
   ```
   export ODDS_API_KEY="<key>"
   bash src/mlb_total_bases_modeling/lambda/deploy_mlb_tb_snapshot.sh
   ```
3. Smoke test passes automatically at end of deploy

## Commit Series (tmp_script.sh)
Run `bash tmp_script.sh` from repo root — 2 commits:
1. feat: explicit ET/UTC column naming across snapshot pipeline
2. chore: pre-commit dev dep + gitignore tmp files

## Naming Convention Decisions (locked)
- **Dates** always have `_et` suffix (`game_date_et`) — ET is house standard for dates
- **Timestamps** always have `_utc` suffix (`snapshot_ts_utc`, `commence_time_utc`) — UTC ISO strings
- **ET string companions** kept alongside every UTC timestamp for human readability in SQL/DuckDB
- `game_date_utc` kept for debugging west coast game date mismatches (shows what old broken [:10] would have given)

## Open Items Carried From Previous Session
- `_print_units_comparison()` in `agg_clv_summary.py` — not yet implemented
- Sep 7 CLV run (after S3 delete + redeploy, data restarts fresh from tonight)
- `plans/20260908-011-mlb-tb-et-utc-naming-conventions.md` — plan doc exists with rationale

## S3 Data Status
All old snapshot data was deleted earlier this session (ET/UTC date was misaligned). After redeploy, Lambda collects fresh overnight. Next morning sync:
```
aws s3 sync s3://the-odds-api-mt/mlb/total_bases_model/prop_snapshots/ ~/Downloads/tmp/total_bases/prop_snapshots/
```
