## Session: 2026-09-07T20-26

## Decisions
- `agg_clv_summary.py` is the aggregate CLV analysis tool — answers "how much edge are we leaving by waiting until 9am ET vs open (first-seen)"
- Merge key: `(player_name, bookmaker, event_id, game_date)` with fallback to `(player_name, bookmaker, game_date)` for old recs without event_id
- `event_id` added to `per_book` in `run_pipeline.py:279` so tomorrow's recommendations.csv will carry it (today's Sep 7 recs don't have it — fallback applies)
- `agg_clv_summary.py` saves two outputs: per-day CSV `clv_summary_{gameday}.csv` and running log `clv_summary_log.csv`, both under `~/Downloads/tmp/total_bases/`
- Sep 8 is the first full overnight CLV day — snapshots running since Sep 7 evening
- Strategy filter: `market_key = batter_total_bases`, `over_line = 1.5`, `under_american_odds IS NOT NULL`
- Lambda `mlb-tb-prop-snapshot` is DEPLOYED and EventBridge rule `mlb-tb-prop-snapshot-hourly` is ENABLED (auto re-enables on redeploy)
- Units +/- section (`_print_units_comparison`) to be built in the AM using Sep 7 data as test bed

## Open Items
- **`_print_units_comparison()` not yet implemented** — build it in the morning using Sep 7 data:
  - Logic: `expected_units_per_bet = novig_under × (decimal_fs - decimal_9am)`
  - `novig_under` from recommendations.csv (already loaded); `first_seen_under_odds` + `nine_am_et_under_odds` from CLV data
  - Need to bring `novig_under` through the merge (add to merge cols alongside key)
  - Sum across all play bets = total expected units gained from first-seen timing vs 9am
- Whether the "edge left on table" is consistently >5¢ across Sep 8 play bets — key question answered after evening run
- Step F (daily verification Lambda) — explicitly deferred
- `verify_snapshot_actionnetwork.py` (Playwright spot-check) — deferred

## Next Actions
1. **Morning** — sync S3 data:
   ```
   aws s3 sync s3://the-odds-api-mt/mlb/total_bases_model/prop_snapshots/ ~/Downloads/tmp/total_bases/prop_snapshots/
   aws s3 sync s3://the-odds-api-mt/mlb/total_bases_model/daily_runs/ ~/Downloads/tmp/total_bases/daily_runs/
   ```
2. **Morning** — run CLV for Sep 7 (games closed):
   `uv run python src/mlb_total_bases_modeling/scripts/compute_clv.py --gameday 2026-09-07`
3. **Morning** — plot a Sep 7 play bet (find player via DuckDB Query 1 with n_play_bets > 0):
   `uv run python src/mlb_total_bases_modeling/analysis/plot_clv_snapshot.py --game-date 2026-09-07 --player "<name>"`
4. **Morning** — run agg_clv_summary for Sep 7 (early read, partial day):
   `uv run python src/mlb_total_bases_modeling/analysis/agg_clv_summary.py --gameday 2026-09-07`
5. **Morning** — implement `_print_units_comparison()` in agg_clv_summary.py using Sep 7 output as test
6. **Morning** — verify Sep 8 hourly snapshots landing:
   `uv run python src/mlb_total_bases_modeling/scripts/verify_snapshot.py`
7. **Evening** (after Sep 8 games close) — sync again, then:
   `uv run python src/mlb_total_bases_modeling/analysis/agg_clv_summary.py --gameday 2026-09-08`
   ← **key run**: verdict on open → 9am timing + units comparison
8. **Evening** — plot Sep 8 play bets, settle Sep 8:
   `uv run python src/mlb_total_bases_modeling/scripts/settle_total_bases.py --gameday 2026-09-08 --output /tmp/sep8_settled.json`

Full checklist: `plans/20260907-010-mlb-tb-clv-tomorrow-checklist.md`

## Key Files / Context
- `src/mlb_total_bases_modeling/analysis/agg_clv_summary.py` — aggregate CLV script (units section TODO)
- `src/mlb_total_bases_modeling/analysis/plot_clv_snapshot.py` — per-player odds movement plot
- `src/mlb_total_bases_modeling/scripts/compute_clv.py` — CLV computation (reads from S3)
- `src/mlb_total_bases_modeling/scripts/run_pipeline.py:279` — event_id now added to per_book
- `plans/20260907-010-mlb-tb-clv-tomorrow-checklist.md` — full morning + evening checklist
- Local data: `~/Downloads/tmp/total_bases/prop_snapshots/` and `~/Downloads/tmp/total_bases/daily_runs/`
- S3: `s3://the-odds-api-mt/mlb/total_bases_model/prop_snapshots/` and `.../daily_runs/`
- Sep 7 insight: Blaze Alexander showed ~30¢ adverse move from 7:31am to first pitch; CLV uniformly +26¢ to +50¢ — strong signal that first-seen timing beats 9am ET
- `~/Downloads/tmp/total_bases/temp_sql_query.sql` — DuckDB queries; Q1 finds players with n_play_bets, Q2 shows per-player snapshots (use to pick which player to plot)
